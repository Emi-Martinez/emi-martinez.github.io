import styled from "styled-components"

const H6 = styled.h6`
    font-family: 'Poppins' ;
    font-weight: bold;
    color: ${props => props.theme.black};
    font-size: 21px;
    line-height: 18px;
    margin: 0;

    ${props => props}
`

export default H6